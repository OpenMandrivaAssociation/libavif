diff -up libavif-1.3.0/src/codec_svt.c.3~ libavif-1.3.0/src/codec_svt.c
--- libavif-1.3.0/src/codec_svt.c.3~	2026-02-28 00:10:25.591032271 +0100
+++ libavif-1.3.0/src/codec_svt.c	2026-02-28 00:11:53.428025176 +0100
@@ -241,6 +241,12 @@ static avifResult svtCodecEncodeImage(av
         // svt_config->avif = (addImageFlags & AVIF_ADD_IMAGE_FLAG_SINGLE) != 0;
 #endif
 
+#if SVT_AV1_CHECK_VERSION(4, 0, 0)
+        // Although the `avif` option was added in v3.0.0, it had a serious bug that was not fixed
+        // until v4.0.0. See https://gitlab.com/AOMediaCodec/SVT-AV1/-/issues/2245.
+        svt_config->avif = (addImageFlags & AVIF_ADD_IMAGE_FLAG_SINGLE) != 0;
+#endif
+
         res = svt_av1_enc_set_parameter(codec->internal->svt_encoder, svt_config);
         if (res == EB_ErrorBadParameter) {
             goto cleanup;
